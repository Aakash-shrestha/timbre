[project]
name = "timbre"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Aakash-shrestha", email = "akash.stha444@icloud.com" }
]
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
    "requests>=2.34.2",
]

[project.scripts]
timbre = "timbre:main"

[build-system]
requires = ["uv_build>=0.11.14,<0.12.0"]
build-backend = "uv_build"
